public enum StageState
{
    Menu,
    Prepare,
    Play,
    End
}

public enum BlockType
{
    Normal,
    Accel,
    Trampolin
}

public enum ObjectType
{
    Normal,
    Accel,
    Trampolin,
    Apple
}